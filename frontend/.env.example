# Proxied to the Laravel dev server by Vite (see vite.config.js).
# Override in .env.local, or point at a deployed origin for production builds.
VITE_API_BASE_URL=/api

# Where the Vite dev proxy forwards /api and /sanctum requests.
VITE_BACKEND_URL=http://localhost:8000
